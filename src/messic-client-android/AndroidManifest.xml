<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.messic.android"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="16"
        android:targetSdkVersion="20" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/messicTheme" >
        <activity android:name=".activities.SplashScreenActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.LoginActivity" >
        </activity>
        <activity android:name=".activities.SearchMessicServiceActivity" >
        </activity>
        <activity android:name=".activities.WelcomeActivity" >
        </activity>
        <activity
            android:name="org.messic.android.activities.BaseActivity"
            android:label="@string/title_activity_base" >
        </activity>
        <activity
            android:name="org.messic.android.activities.AlbumInfoActivity"
            android:parentActivityName="org.messic.android.activities.BaseActivity" >

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="org.messic.android.activities.BaseActivity" />
        </activity>

        <service
            android:name="org.messic.android.player.MessicPlayerService"
            android:exported="false" />
        <service
            android:name="org.messic.android.download.DownloadManagerService"
            android:exported="false" />
    </application>

</manifest>